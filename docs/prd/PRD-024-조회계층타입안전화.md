# PRD-024 — 조회 계층 타입 안전화

**이슈**: [#130](https://github.com/swyp-app-6th-3rd-team/Pickple-backend/issues/130)

## 무엇을 왜 만드는가

조회 저장소가 **네이티브 SQL 문자열 + `List<Object[]>` + 컬럼 인덱스 상수**로 짜여 있다.
이 조합의 대가는 셋이다.

1. **런타임에만 깨진다.** SELECT 절에 컬럼을 끼워 넣으면 뒤 인덱스가 전부 밀리는데 컴파일은 통과한다.
2. **문자열 SQL 조립.** 컬럼명·정렬 방향을 `.append()`·`.formatted()` 로 잇는다. 지금은 enum 필터가
   막지만 방어가 **호출자 규약에 의존**한다.
3. **선례 복제로 번진다.** 인덱스 상수 클래스가 파일마다 반복된다.

`Object[]` 는 증상이고 원인은 **조회 계층 표준의 부재**다. `QuerydslConfig` 에 `JPAQueryFactory` 빈이
있고 Q클래스 16개가 생성되는데 사용처가 0건이었다 — 배선만 해두고 아무도 쓰지 않았다.

## 범위

### 포함

| # | 대상 | 상태 |
|---|---|---|
| 1 | `RankingListRepository` → QueryDSL (파일럿) | ☑ 완료 |
| 2 | `JpaGradeStore` 프로젝션 교체 | ☑ 완료 ([#132](https://github.com/swyp-app-6th-3rd-team/Pickple-backend/issues/132)) |
| 3 | `PostListRepository` 2쿼리 분할 ([#138](https://github.com/swyp-app-6th-3rd-team/Pickple-backend/issues/138)) | ☑ 완료 |
| 4 | `ActivityListRepository` 2쿼리 분할 ([#133](https://github.com/swyp-app-6th-3rd-team/Pickple-backend/issues/133)) | ☑ 완료 |
| 5 | `RandomPostRepository` | ☐ 미착수 |
| 6 | 게시글 상세 조회 (#20) | ☐ 미착수 |

### 제외 — 사유를 각 파일 javadoc 에 남긴다

| 대상 | 제외 사유 |
|---|---|
| upsert 3종 (`INSERT IGNORE`, `ON DUPLICATE KEY UPDATE`) | QueryDSL-JPA 에 upsert API 자체가 없다. JPA 한계지 선택이 아니다 |
| 원자적 카운터 6개 (`PostCounterRepository`) | `GREATEST(CAST(…))` 템플릿이 필요해 이득 대비 위험이 크다 |
| `JpaRankingStore` 윈도 함수 | multi-table UPDATE + `ROW_NUMBER() OVER` 는 JPQL 로 표현 불가 |
| `CommentRepository` | 네이티브지만 **이미 인터페이스 프로젝션**이라 타입 안전하다 |

`CommentRepository` 를 제외한 것이 이 PRD 의 판단이다. "네이티브 쿼리" 를 기준으로 잡았다면
멀쩡한 코드를 건드렸을 것이다 — **판별 기준은 네이티브 여부가 아니라 `Object[]` 사용 여부**다.

## 완료 판정

| 판정 | 검증 방법 | 현재 |
|---|---|---|
| 조회 경로 `Object[]` 0건 | `grep -rn "Object\[\]" src/main/java/` (주석·제외 경로 빼고) | 38 → 15 → **8** (#3 반영, 병합 시점 실측. 남은 8은 전부 `RandomPostRepository`·`JpaPostStore` 랜덤 경로 — #5) |
| 컬럼 인덱스 상수 클래스 0개 | `grep -rn "static final int .* = [0-9]"` | 랭킹 6개 + 활동 11개 + 목록 13개 제거 (`RandomPostRepository.Column` 만 남음 — #5) |
| 컬럼명·정렬의 문자열 SQL 조립 0건 | `.append(sortColumn` · `.formatted(` · `.substring(2)` 검색 | 랭킹·등급·활동·목록 0건 (`RandomPostRepository` 의 `.formatted(` 잔존 — #5) |
| `toLong`/`toCreatedAt` 중복 제거 | 복제본 2곳 → 0 | ☑ `toLocalDateTime`·`toCreatedAt`·`toRanking` 제거 (#3). `toLong` 은 랜덤 경로가 아직 쓴다 — #5 |
| **성능 회귀 없음** | 실행계획이 기존 인덱스 사용 + **절대값 병기** | ☑ 랭킹(#1) · ☑ 등급(#2) · ☑ 목록 2쿼리(#3) · ☑ 활동 2쿼리(#4) |
| 기존 테스트 전부 통과 | 전체 스위트 | ☑ 실패 0건 |

## 측정 기록

### 1. `RankingListRepository` → QueryDSL

**실행계획 (전환 후)**

```
-> Limit: 21 row(s)  (cost=0.71 rows=1)
   -> Index range scan on u using idx_users_ranking_order over (10 < ranking)
      index condition: ((u.ranking not null) and (u.ranking > 10))
```

`idx_users_ranking_order` **range scan** 유지. filesort·풀스캔 0건.
V10 마이그레이션이 기록한 기준(200k 회원에서 33ms → 0.060ms)의 인덱스를 그대로 탄다.

**회귀 방지**: 일회성 확인으로 끝내지 않고 `RankingControllerIT.sliceUsesRankingIndex` 로 남겼다.
정렬 키를 `u.point` 로 바꿔 **위반을 주입했을 때 실제로 실패**하는 것을 확인했다 —
통과만으로는 보호의 증거가 되지 않는다.

**제거된 것**: `Object[]` 6곳 · `Column` 상수 6개 · `toLong`/`toRanking` 방어 함수 2개 · 149줄 저장소

### 2. `JpaGradeStore` 프로젝션 교체 (#132)

**QueryDSL 로 옮기지 않았다.** `READ_INPUTS` 는 FROM 절 없는 스칼라 서브쿼리 SELECT 라 JPQL 문법에
없다. SQL 은 그대로 두고 **반환 타입만** 바꿨다 — 파일럿의 `Projections.constructor` 규약은 JPQL 전제라
여기 적용되지 않는다.

**매핑 방식: Spring Data 인터페이스 프로젝션** (`CommentRepository.CommentListRow` 선례).
record 매핑을 기각한 이유는 드라이버 타입이다 — `SUM()` 이 `BigDecimal` 로 오는데 Hibernate 의
네이티브 record 매핑은 생성자 인자 타입과 드라이버 타입이 맞아야 한다. 맞추려면 SQL 에 `CAST` 를
넣거나 record 가 `BigDecimal` 을 들어야 해서, 어느 쪽이든 `toLong` 이 막던 문제가 계약으로 새어 나온다.
인터페이스 프로젝션은 접근자 반환 타입에 맞춰 `ConversionService` 가 변환한다
(`spring-data-commons 4.1.0` `ProjectingMethodInterceptor.potentiallyConvertResult` 소스 확인).
`highest_grade`(TINYINT) 스칼라는 `JpaQueryMethod.potentiallyUnwrapReturnTypeFor` 가 `Optional<Integer>` 를
`Integer` 로 벗긴 뒤 `JpaQueryExecution.execute` 가 같은 `ConversionService` 로 변환한다(이종 리뷰가 바로잡음 —
초안은 `QueryExecutionResultHandler` 로 적었다). 오버플로는 이전 `BigDecimal.longValue()` 가 조용히 wrap 하던 것과
달리 `ConversionFailedException` 으로 드러난다 — 비트 단위 동일이 아니라 더 안전한 쪽으로의 차이다.

**ADR 을 쓰지 않았다.** 두 파일·한 패키지에 갇힌 결정이고 스키마·API 계약이 바뀌지 않으며, 방식을
바꾸더라도 리포지토리 인터페이스 하나만 고치면 된다. 근거는 `GradeRepository` javadoc 에 있다.

**실행계획 (교체 후 — SQL 이 같으므로 교체 전과 동일)**

```
-> Rows fetched before execution  (cost=0..0 rows=1)
-> Select #2 (subquery in projection; run only once)
    -> Aggregate: sum(ph.amount)  (cost=0.45 rows=1)
        -> Index lookup on ph using idx_point_user_created (user_id=1)  (cost=0.35 rows=1)
-> Select #3 (subquery in projection; run only once)
    -> Aggregate: count(0)  (cost=0.45 rows=1)
        -> Covering index lookup on v using idx_vote_user_created (user_id=1)  (cost=0.35 rows=1)
```

두 서브쿼리 모두 `user_id` 선행 인덱스 lookup, 테이블 스캔 0건. 투표 횟수는 커버링 조회다.

**회귀 방지**: `JpaGradeStoreIT.readInputsUsesUserIndexes` 가 `GradeRepository.READ_INPUTS` 상수를
**그대로** EXPLAIN 한다(SQL 을 베끼지 않아 문장이 바뀌면 함께 검사된다). 위반 주입 결과:

| 주입 | 결과 |
|---|---|
| `WHERE ph.user_id + 0 = :userId` (인덱스 무력화) | 9개 중 **1개 실패** — EXPLAIN 단언만 |
| `AS point` → `AS points` (별칭 드리프트) | 9개 중 **8개 실패** — 접근자가 null 을 돌려줘 첫 호출에서 NPE |

두 번째 행이 인터페이스 프로젝션의 트레이드오프다 — 이름으로 묶이므로 컴파일은 통과하지만,
`readInputs` 를 부르는 테스트 전부가 첫 호출에서 깨진다.

**동작 보존**: 사용자가 없을 때 이전 구현은 `NoResultException` 을 그대로 흘려 500 이었다. Spring Data 는
빈 값을 돌려주므로 `JpaGradeStore` 가 `GradePersistenceException` 을 던진다 — 각 기능 `infra` 의
`*PersistenceException` 규약(ADR-0039)을 따르며 응답은 같은 500 이다. 리뷰(동종)가 지적해 raw JPA 예외
재던지기에서 바꿨다. `JpaGradeStoreIT` 10건 · `GradeServiceIT` 7건 통과.

**제거된 것**: `Object[]` 1곳 · 인덱스 리터럴 `row[0]`·`row[1]` · `toLong` · `EntityManager` 직접 사용

### 3. `PostListRepository` → QueryDSL 2쿼리 분할 (#138)

**실행계획 (전환 후)** — 게시글 20,000건 · MySQL 8.4 · `EXPLAIN ANALYZE`, 커서는 정렬의 한가운데(10,000번째).
옛 문장과 새 문장 모두 `StatementInspector` 로 붙잡아 같은 값을 바인딩했다.

| 경로 | 전 (네이티브 1문장) | 후 ① 키 | 후 ② 행 | 계획 |
|---|---|---|---|---|
| 최신순 첫 조각 | 0.137 ms | 0.039 ms | 0.109 ms | `idx_post_latest_all` 11행 |
| 최신순 둘째 조각 | 5.12 ms | 0.783 ms | 0.043 ms | 같은 인덱스, 행 값 비교는 전후 모두 `Filter`(커서 앞 10,012행) |
| 인기순 둘째 조각 | 5.45 ms | 1.39 ms | 0.091 ms | `idx_post_popular_all` |
| 카테고리 최신순 둘째 조각 | 1.10 ms | 0.255 ms | 0.059 ms | `idx_post_latest`(카테고리 선행) |

```
-> Limit: 11 row(s)
   -> Filter: ((pe1_0.deleted_at is null) and ((pe1_0.created_at,pe1_0.id) < (?,?)))
      -> Covering index lookup on pe1_0 using idx_post_latest_all (deleted_at=NULL)
```

**인덱스를 잃거나 `Sort:` 가 새로 생긴 경로가 없다.** 둘째 조각이 전보다 빨라진 것은 목표가 아니라 부산물이다 —
옛 안쪽 질의는 파생 테이블에 실을 열 컬럼을 전부 골라 커서 앞 인덱스 항목마다 클러스터 행을 읽었고, 새 키 문장은
`id` 만 고르므로 커버링으로 끝난다. ② 는 네 경우 모두 `PRIMARY` 범위(10 ids) → `users` 기본 키 단건 조회이며
대표 사진 서브쿼리는 정확히 10번 돈다. 왕복은 요청당 1 → 2. 행 수에 비례하는 문장은 여전히 없다.
**"② 는 게시글 기본 키에서 시작해야 한다" 는 열린 질문은 실측으로 닫혔다** — 옛 한 문장의 바깥 질의는 실제로
`Table scan on u` 에서 시작했고(작성자 1명 시드), 새 행 문장은 기본 키 범위가 진입점이다.

**회귀 방지**: `PostControllerIT.QueryPlan` 5건 + `PopularPostsIT` 실행계획 1건. 손으로 베낀 SQL 이 아니라
**Hibernate 가 실제로 내보낸 SQL** 을 `SqlCapture` 로 붙잡아 같은 값을 바인딩해 EXPLAIN 한다. 이 파일이 갖고 있던
손으로 베낀 EXPLAIN 두 곳(`explainPostList`·`explainPopular`)도 캡처 문장으로 바꿨다. 위반 주입 결과:

| 주입 | 결과 |
|---|---|
| 정렬 튜플의 `id` 방향을 `ASC` 로 | **4건 실패** — `Sort:` 가 나타난다 |
| 행 문장을 `FROM users JOIN post` 로 | **실패 없음** — 계획이 같다. `FROM` 은 조인 순서를 강제하지 않고, 테스트는 문장이 아니라 계획을 고정한다 |

**문장 수 테스트 셋을 고쳤다.** "슬라이스당 1문장" 을 절대값으로 박아 두던 `PostControllerIT` 2건·`PopularPostsIT` 1건이
2 로 깨졌다. 지키려던 성질은 숫자가 아니라 행 수에 비례하지 않음이므로 #4 의 형태(성질이 주 단언, 상수 2 가 부 단언)로
바꿨고, 작성자 수에 비례하지 않음을 실제로 재도록 `rankingAddsNoQuery` 는 작성자 넷으로 나눠 재조준했다(ADR-0045).

**제거된 것**: `Object[]` 2곳 · `Column` 상수 13개 · `.append(sortColumn` 3곳 · `.formatted(` 1곳 · `.substring(2)` 1곳 ·
`toLocalDateTime`/`toCreatedAt`/`toRanking` 3개 · 158줄 저장소. **새로 생긴 것**: 요청당 왕복 1 · 트랜잭션 전제(저장소가 단언).

### 4. `ActivityListRepository` → QueryDSL 2쿼리 분할 (#133)

**실행계획 (전환 후)** — 활동 5,000건 · 게시글 20,000 · MySQL 8.4 · `EXPLAIN ANALYZE`, 커서는 2,500번째 활동.

| 경로 | 전 (네이티브 1문장) | 후 ① 키 | 후 ② 행 | 계획 |
|---|---|---|---|---|
| 투표 최신순 첫 조각 | 0.031 ms | 0.020 ms | ≈0.04 ms | `Covering index lookup … idx_vote_user_activity` 11행 |
| 투표 최신순 둘째 조각 | 0.258 ms | 0.233 ms | 0.037 ms | 같은 인덱스, 행 값 비교는 전후 모두 `Filter` |
| 댓글 최신순 둘째 조각 | 0.218 ms | 0.227 ms | 0.035 ms | `idx_commenter_user_activity` |
| 투표 인기순 둘째 조각 | 3.45 ms | 3.87 ms | 0.046 ms | `Sort` + 5,000행 — ADR-0036 이 받아들인 Θ(내 활동 수) |

```
-> Limit: 11 row(s)
   -> Nested loop inner join
      -> Covering index lookup on ve1_0 using idx_vote_user_activity (user_id=1)  rows=11
      -> Single-row index lookup on pe1_0 using PRIMARY (id=ve1_0.post_id)
```

**계획이 바뀐 경로가 없다.** `Sort:` 가 새로 생기거나 인덱스를 잃은 곳이 없고, ② 는 네 경우 모두
`PRIMARY` 범위(10 ids) → `uk_*_post_user` 단건 조회이며 대표 사진 서브쿼리는 정확히 10번 돈다.
왕복은 요청당 2 → 3 (키 · 행 · 탈퇴 차단 관문). 행 수에 비례하는 문장은 여전히 없다.

**회귀 방지**: `ActivityControllerIT.QueryPlan` 8건. 손으로 베낀 SQL 이 아니라 **Hibernate 가 실제로 내보낸
SQL** 을 `StatementInspector`(`SqlCapture`)로 붙잡아 같은 값을 바인딩해 EXPLAIN 한다. 정렬 튜플의 두 번째
자리를 `p.id` 로 바꿔 위반을 주입했을 때 최신순·오래된순 두 건이 실제로 실패했다.

**제거된 것**: `Object[]` 6곳 · `Column` 상수 11개 · `.formatted(` SQL 조립 3갈래 · `toLong`/`toLocalDateTime`
방어 함수 2개 · 271줄 저장소. **새로 생긴 것**: 요청당 왕복 1 · 트랜잭션 전제(저장소가 단언) ·
`PostEntity.popularityScore` 읽기 전용 매핑.

## 열린 질문

- **2쿼리 분할의 성능 영향** (#3·#4·#5) — `FROM (SELECT…LIMIT)` 파생 테이블을 QueryDSL-JPA 가
  지원하지 않아 ① id 목록 → ② `WHERE id IN (…)` 로 나눈다. **#4 에서 실측으로 판정했다** — 아래
  측정 기록 4 와 ADR-0043. 활동 5,000건에서 네 경로 모두 실행계획이 전후 동일했고 후퇴안은 쓰지 않았다.
  #3·#5 는 같은 방법(실제 SQL 을 붙잡아 EXPLAIN, 전후 절대값 병기)으로 각자 판정한다.
  ② 는 **게시글(기본 키)에서 시작**해야 통계와 무관하게 Θ(조각 크기) 가 된다 — #3 도 같은 방향으로 실측 확인했다
  (측정 기록 3). 남은 것은 #5 뿐이다.
- `popularity_score` 매핑은 실측했으나(ADR-0041), `active_nickname` 은 아직 매핑하지 않았다.
  #3 은 필요하지 않았다 — 목록의 작성자 표시명은 `nickname`·`name` 폴백이라 매핑된 두 컬럼으로 족하다.

## 변경 이력

- 2026-09-07 신규. 파일럿(#1) 완료.
- 2026-09-07 #2 `JpaGradeStore` 완료 (#132). ADR 미작성 사유 기록.
- 2026-09-07 #4 완료(#133). 2쿼리 분할 실측 판정 — 회귀 없음, 후퇴안 미사용.
- 2026-09-08 #3 완료(#138). 선례 원본 전환, 문장 수 계약 1 → 2 (ADR-0045).
